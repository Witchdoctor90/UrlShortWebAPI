import React, {useState} from 'react';

const Info = () => {
    const[isAuthenticated, setIsAuthenticated] = useState(false);
    return (
        <div>
            
        </div>
    );
};

export default Info;